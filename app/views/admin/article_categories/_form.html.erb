<%= simple_form_for [:admin,@article_category] do |f| %>
  <div class="form-group">
    <%= f.input :name, input_html: { class: "form-control"}, :label => "文章类别" %>
  </div>
  <%= f.submit "提交", class: "btn btn-primary", data: { disable_with: "Submiting..." } %>
<% end %>