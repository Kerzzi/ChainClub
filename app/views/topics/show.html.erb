<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_topic_path(@topic), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @topic.title %></h2>
  <p><%= @topic.content %></p>
</div>